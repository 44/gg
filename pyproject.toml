[project]
name = "gg"
description = "Collection of tools/scripts"
requires-python = ">=3.10"
dependencies = ["rich"]
dynamic = ["version"]

[project.scripts]
gg = "gg:main"

[tool.hatch.version]
path = "gg/__init__.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
